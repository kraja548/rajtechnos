#include<stdio.h>
main()
{
    enum channel {star, sony, zee};
    enum symbol {hash, stars};
    int i;
 	//printf("%d",hash);
	//printf("%d",star);
	for(i = star; i <= zee; i++)
    {
        printf("%d", i);
    }
}